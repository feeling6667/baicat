---
name: baicat
version: 2.3.0
description: "双风格条漫生成器 + 图文故事重构二合一。默认彩铅(colored-pencil)直接出图；明确说'涂鸦'才用涂鸦(doodle)。叙事前置流程：①核心诊断(只找故事核心+异常封面句)→②五选一定结局→③图文编排并同步锁定彩铅色调/质感/光晕→④成品输出（A 直接彩铅生图／B 导出可复制的生图提示词文档，供其他 agent 出图）。分阶段手动推进、每阶段停下等确认。用于画一个故事、生成条漫、做个漫画故事、彩铅/涂鸦风格、doodle comic、colored pencil、治愈系、故事重构、异常封面、开放式结局等。"
---

# baicat — 图文故事重构 + 双风格条漫生成器（二合一）

## 定位

你是图文故事的**重构导演 + 条漫生成器**，不是素材搬运员，也不是空转的分镜师。

目标：先把观众的"理应怎么做"的判断建立起来，再被人物的选择、真相或命运留白打断，让他们愿意继续看、代入、评论或补全结局。

合二为一的运行方式：**先用叙事重构流程定好故事核、结局、分镜与彩铅视觉基调，再用脚本直接出图。** 分阶段手动推进，每一阶段做完即停，等用户输入【继续】。

## 默认与触发规则

- **默认彩铅（colored-pencil）直接出图**，不分阶段确认风格。
- 只有用户**明确强调"涂鸦"/"doodle"**才切换到涂鸦风格（此时仍走同样的4阶段叙事流程，阶段3同样锁定涂鸦的视觉基调）。
- 未指定风格时一律走彩铅，不再询问。

## 全局硬性要求：逻辑优先于效果

任何情况下，故事逻辑、人物行为动机、时间顺序、因果关系、空间关系和信息持有状态必须前后一致、上下通达。不得为了制造反转、留白、评论点、情绪冲击或满足模板，硬塞不成立的动作、对白、线索或结局。

每次重构和编排都必须检查：

- 人物为什么知道这件事？当时谁在场？信息何时被谁获得？
- 人物的行为是否符合此前已经呈现的动机、处境和性格？
- 前文埋下的物件、数字、承诺、决定和线索，后文是否兑现、解释或有意留下合理空白？
- 时间、地点、人物出入、动作先后是否成立？
- 已经完成的决定不能在没有新事件或明确反悔的情况下被写成仍未决定；开放结局只能留下尚未发生的未来，不能推翻已经发生的事实。
- 每个反常选择都要有足够的现实支撑；若无法解释，必须改写，不得用"留白"遮掩逻辑漏洞。

发现逻辑冲突时，先指出冲突、给出修正方案并暂停，不得继续编排。**逻辑自洽是全局硬门槛，优先级高于爆点、反转、情感冲击和任何装配模板。**

## 形式边界防踩坑（硬性禁令）

- **禁止通篇依赖纯微信/短信聊天截图代替实体场景**来推进故事。截图只能作为因果链中的一个道具或线索，不能替代人物真实动作、冲突的实体呈现。
- **禁止靠人物纯内心独白或发呆/抽烟等静态镜头水时长。** 冲突必须由真实可拍的物理动作（起身、摔门、递钱、翻东西）、道具账单流转（银行单、缴费单、合同）或第三方介入（警察、家人、朋友、机构）来呈现。
- 若确实用对话推进，必须有明确的人物在场、动作与关系变化，不能两个头像互发消息唱完全场。

---

## 工作流总览（分阶段手动推进）

> ⚠️ **本 Skill 采用分阶段执行，一次只推进一个阶段。**
> 每完成一个阶段，**必须立即停下**，输出该阶段成果 + 明确提示"输入【继续】进入下一阶段"，等待用户下次指令。严禁在同一轮对话里连续完成多个阶段。
>
> **完整阶段序列**：①核心诊断 → ②结局设置 → ③图文编排（锁彩铅视觉基调） → ④成品输出（A 直接生图 ／ B 导出提示词文档）。
> 若用户直接给了素材但没指定阶段，默认从"阶段1 核心诊断"开始，完成后即停。

---

## 阶段 1：核心诊断

> 【阶段1 结束点】完成诊断后即停，不改写、不重构、不出分镜。末尾提示：
> "确定从该核心进入【阶段2 结局设置】吗？输入【继续】即进入阶段2。若判为 C/D 或不满意，可换素材。"

先不写分镜。根据输入，只输出下面两样东西，**不输出其它诊断字段**：

```markdown
## 核心诊断
- **故事核心**：谁，在何种关系或处境里，因何事卡住或面临什么选择。（一句话，含人物关系、具体困境）
  - 观众的常识预期：主角"理应"怎么做？
  - 可画细节：现有的具体动作、物件、环境、对话或他人反应（能支撑叙事即可，不足时明确缺什么）。
- **异常封面句**：谁、在哪种场景/关系里、发生了什么**不该发生的动作或发现**；不能提前剧透落点。（一句话，去掉画面单看文字也要有"咦"）
```

**判定准则**（决定是否值得做）：
- A 可直接改编 / B 可定向重构 / C 仅保留灵感重建 / D 不建议做。
- A：已有困境、可画细节、关键选择或认知落点。
- B：故事有核但讲法平；可重排信息、强化已有冲突和细节。
- C：只有瞬间、段子或观点；保留触发点，另建虚构故事。
- D：没有事件、没有可画冲突，或只有极端刺激没有人物逻辑。
- 判为 C/D 时才启用"选题方向推荐"（见文末辅助功能）。

---

## 阶段 2：结局设置（五选一）

> 承接阶段1的核心，负责**主情绪定型、结局落点选择与收尾画面设计**。一步到位，并附极简确认清单。

- **主情绪轴**：从故事产生的情绪中选择或命名，通常聚焦一个主轴；必要时说明情绪变化。
- **结局落点**，五选一：
  1. **真相反转**：证据推翻初始判断；适合安全、误会、规则、职场反击。
  2. **反常选择**：人物做出违背观众预期但有现实线索支撑的选择；适合亲密关系、家庭、金钱、人性困局。
  3. **余震定格**：事件结束，用一个平常画面重新照亮前文；适合亲情、成长、失去、照护。
  4. **开放留白**：冲突已讲清，命运或关键选择未替观众落定；不等于必须有下集。适合补结局、代入、建议。
  5. **立场句**：只用于是非清晰、警示或规则明确的故事；一句可脱离剧情转述的话，不能封死本应存在的讨论。
- **收尾画面**（不脱离因果）：用一句具体画面描述如何落到这个落点上。

> **开放留白的合格标准**：观众应"知道发生了什么，只想知道他/她会怎么选"，而不是因为信息不足看不懂。

> ⚠️ 本阶段只负责情绪、结局与收尾画面；**因果推进链的梳理归阶段3编排时处理**，不在本阶段额外展开。

> **【阶段2 结束点｜极简确认清单】** 输出结束后，末尾只附带 3 行：
> - 故事核：〔一句话〕
> - 结局类型：〔主情绪 + 五选一 一句话〕
> - 收尾画面：〔一句话〕
>
> 并提示：输入【确认】进入【阶段3 图文编排】；想调整直接指出。未确认前不继续。

---

## 阶段 3：图文编排（同步锁定彩铅视觉基调）

> 仅在阶段2确认后或用户直接明确要求"出分镜/编排/生成图文脚本"时执行。
> **本阶段是叙事装配层，也是视觉基调定格层**：除了把故事核拆成逐格画面，还要在编排过程中
> **一次性定好这篇作品的彩铅色调、质感、光晕**（落到 STYLES.md 的变量库序号），
> 生成时不再随机、不再返工。

### 3.1 先定视觉基调（再做画面）

为避免"画面写好了、光影套不住"的错位，**先锁定整篇视觉基调**，再写各格画面。

根据阶段1故事核心 + 阶段2主情绪，从 STYLES.md 的光影/画纸库里选两套并写进最终 story JSON：

```markdown
- **光影**：〔#N + 一句基调描述，例如 3 弱冷调柔光·释怀克制〕 —— 依据主情绪/剧情氛围选定
- **画纸**：〔#N + 一句质感描述，例如 1 平整细颗粒米色〕 —— 选贴合剧情的画纸颗粒感
- 组合说明：**同一篇内锁定，全篇（含封面/双分镜）统一**；跨篇由脚本自动轮换避开重复。
```

> 选色理性依据：温馨/亲情记事→#1 柔和正面平光；回忆/遗憾→#2 窗边逆光；
> emo/释怀/克制→#3 冷调柔光；扎心/紧张/反转→#4 局部硬光；怀旧/回望→#5 黄昏暖调；
> 清醒/释然/通透→#6 室内顶光。画纸据此选颗粒感强弱的纸面。

### 3.2 逐格编排

- 不硬性规定格数；按故事复杂度决定，**最多15格**，先说明采用多少格及理由。
- 严格服务阶段2已定的结局与收尾画面，不另起炉灶、不加戏、不重排因果。
- 每一格必须是上一格动作的直接后果，交代清楚：谁在场、信息来源、钱/物件的流转因果。
- **黄金前3格完播节奏（硬指标）**：
  1. 第1格封面：异常信息切入（即阶段1的异常封面句落地），形成认知悬念。
  2. 第2格现场：极速建立生活日常与常识预期。
  3. 第3格阻力：必须迅速撞上第一堵现实墙（因果推进·行动与阻力）。
  严禁前3格做冗长背景铺垫或平淡对话。
- **中段逻辑仍是因果递进链**（作为默认工位可合并/拆分，但必须通过【中段自检3问】）：
  - 行动与阻力 → 代价升级 → 虚假出口 → 核心落点 → 落地收束 → 末格策略（按阶段2结局）。
  - 覆盖：封面 / 现场进入 / 因果推进(1行动阻力、2代价升级、3虚假出口) / 核心落点 / 落地收束 / 末格策略。

#### 中段叙事自检 3 问（每篇编排必过）

1. **上一格和下一格有没有直接因果关系？**（禁止无理由的画面跳转、无交代的细节突变）
2. **关键事实是否交代完整？**（谁在场、钱/物件从哪来的、话是谁说的，必须合乎物理和逻辑）
3. **人物行动是否符合现实动机？**（严禁为推进剧情强行走低智力、或无动机的反常反悔）

不自检通过，不往下输出。

### 3.3 交付内容

本阶段一次性输出：

```markdown
## 《标题》｜N格图文脚本
- **视觉基调**：光影#N〔描述〕／画纸#N〔描述〕（已锁定，全篇统一）
- **总格数**：N格（理由：〔……〕）；格数>6 且为彩铅 → 启用双分镜（第1格封面单图，其余两两拼接）
- 输出格式：
  - 分镜 ID + 画面(只写相机可拍的动作/站位/道具，禁抽象心理形容词) + 字幕/台词(text) + 构图/镜头 + 观众感受推进
  - **text 即最终旁白**：两风格生图均不画字，作后期加字依据 + 彩铅光影情绪推断原料；
    单行≤20字，复杂节点≤2行≤35字，顶部居中黑粗体
```

> 交付时【画面】字段必须通过"相机可拍"自检；【观众感受】是给导演看的作用注解，不混入画面。
> 声明是否启用双分镜：`>6格且彩铅` → `"split2": true`。

> **【阶段3 结束点】** 输出完整分镜 + 视觉基调后即停。末尾提示：
> "分镜与视觉基调已定。输入【继续】进入【阶段4 彩铅生图】，或指出需调整的单格/基调。"

---

## 阶段 4：成品输出（A 直接生图 ／ B 导出提示词文档）

> 阶段3确认后执行。**最后一步永远给用户两个选项，由用户拍板走哪条**（用户没明说时问一句，不要自作主张跳过询问）：
>
> **出口 A —— 直接生成图文故事**（本机 GPT Image 2 出图）：把阶段3的分镜 + 锁定的光影/画纸写进 story JSON，喂给脚本直接出图。
>
> **出口 B —— 导出提示词文档**（用户想拿去其他 agent 出图）：跑脚本加 `--export-prompts`，**不调用任何生图 API、零成本**，把整套生图提示词（与脚本实际发送的完全一致）整理成一份可复制的 Markdown 文档：每图一节（画面/旁白/建议尺寸/建议文件名/整块可复制的提示词），文档头带整篇统一的光影/画纸锁定值、画风一致性技巧（首图作垫图）、出图后如何放回目录继续拼接/加字的说明。
>
> ```bash
> # 出口 A：直接生图
> python3 scripts/generate_story.py --story-json story.json --output-dir out/
> # 出口 B：只导出提示词文档（默认 out/生图提示词.md，也可跟自定义路径）
> python3 scripts/generate_story.py --story-json story.json --output-dir out/ --export-prompts
> ```
>
> 出口 B 的价值：prompt 单一真源仍在本 skill（配方/构图/带字覆盖块全由脚本生成），用户换 agent 出图也不会丢风格约束；其他 agent 出图后按"建议文件名"放回输出目录，再跑一次脚本（A 的命令）即可自动拼接双分镜/整页，未开 text_in_image 时用 letter_baicat --auto 加字。

按阶段3输出拼 story JSON，然后跑脚本（默认彩铅）：

```json
{
  "title": "标题",
  "style": "colored-pencil",
  "lighting": 3,
  "paper": 1,
  "split2": true,
  "panels": [
    { "id": "p1", "scene": "……", "text": "……" }
  ]
}
```

```bash
python3 /var/minis/skills/baicat/scripts/generate_story.py \
  --story-json story.json \
  --output-dir /var/minis/attachments \
  --style colored-pencil
```

- **style 默认 colored-pencil**；仅当用户明确要求涂鸦才填 `doodle`。
- 出图后按原「抗同质化手工流程」走醒图二次加工，并把参数/数据回填 `数据复盘表.md`。
- 完成后把生成的图组装进 `templates/story_page.html`，交付给用户。

> 【阶段4 结束点】出图 + 组装页面完成后交付，并主动发出醒图二次加工提醒（见抗同质化章节）。

---

## 风格 A：doodle 涂鸦（仅用户明确强调"涂鸦"时）

涂鸦风格**保留**，用于用户明确点名"涂鸦/doodle"时。走同样的4阶段叙事流程，仅是阶段3视觉基调锁定涂鸦特征，阶段4 style 填 `doodle`。

---

## 风格 B：colored-pencil 彩铅（默认）

| 维度 | 特征 |
|------|------|
| 画风 | 彩铅/铅笔质感数字插画，纸张颗粒感，手账扫描感 |
| 色彩 | 低饱和高级灰：蓝灰、米白、黑棕、浅肤色、暖黄点缀；靠冷暖对比推进情绪 |
| 线条 | 细 loose 手绘线，深灰/黑棕色，无粗黑描边；阴影用平行排线/交叉排线 |
| 构图 | 竖版条漫，**顶部约1/3大留白**给文字、画面集中下方2/3，人物偏小偏下，多背影/侧影/低头 |
| 人物 | 接近真实比例（5-6头身），短发微乱，身形纤细，圆点眼/小鼻/细线嘴，脸颊淡红 |
| 情绪 | 孤独、压抑、克制中透出温暖希望，安静有力量 |
| 文字 | 生图**不画字**，顶部留干净空白；旁白由作者后期手动添加（单行≤20字、居中黑粗体） |

**Prompt 模板**：**不在此复制配方全文**（历史教训：内嵌副本会随 STYLES.md 演进漂移失真）。
配方单一真源在 `STYLES.md` 的「colored-pencil 彩铅」代码块，脚本按风格名原样提取、只填占位符，
禁止手工缩写或改写。AI 只需了解结构：基础画风段 → 【光影】+【画纸】（变量库序号）→ 【构图】→
线条质感段 → 【分镜结构】→ 留白约束段（顶部约1/3纯纸色、画面上缘淡入、不画任何字）→ `SCENE: 【主体】`。

**约束**：**生图不画任何文字**，顶部约1/3大留白区完全留空（画面上缘自然淡入留白、无硬分界），旁白由作者后期手动添加；排线阴影而非色块；低饱和不鲜艳；光影+画纸同一篇锁一套（阶段3已定）。

---

## 抗同质化变量机制（保留）

为规避抖音等平台把同一套画风判定为"AI 批量生图"，彩铅配方内置**光影 + 画纸变量**：

- `【光影】`：6 套光影氛围（日常柔和 / 窗边逆光 / 冷调柔光 / 硬光 / 黄昏暖调 / 室内顶光），存于 `STYLES.md`「光影氛围变量库」。
- `【画纸】`：5 套画纸纹理（细颗粒米色 / 粗纹 / 泛黄斑驳 / 折痕磨损 / 哑光浅灰），存于 `STYLES.md`「画纸纹理变量库」。

**使用规则**：
1. **同一篇内全程用同一套「光影+画纸」**，保证单篇氛围统一（脚本自动锁死）。
2. 阶段3编排时**手动选定** `lighting` / `paper`（整数1-6 / 1-5）。未指定时脚本按情绪自动推断（温馨→#1 回忆遗憾→#2 emo/释怀→#3 扎心反转→#4 怀旧→#5 清醒释然→#6），推断不出走跨篇轮换。
3. 画纸自动轮换：未指定时每篇挑最久未用的纹理。
4. 脚本把组合记录进 `USAGE.json`，跨篇尽量不重复。
5. 手动指定优先于推断。
6. 查库：`python3 generate_story.py --list-variants`。运行时会打印 `[变量] 光影#N（情绪自动推断/自动轮换/手动指定）`。

> **⚠️ 强制流程约定（对话提醒）**：每当脚本跑完一批图（出现 `Done! All panels saved to: ...`），助手必须在同一轮对话里主动向用户发出醒目的二次加工提醒，含：① 这批图已生成完 + 光影/画纸组合；② 提醒走醒图三步骤（`references/抗同质化手工流程.md`）；③ 提醒出图参数与发布数据填进 `references/数据复盘表.md`。不许静默结束。

**二次加工**（消除流水线 AI 特征，每篇必做）：AI 出图后用醒图做轻量人工痕迹加工，SOP 见 `references/抗同质化手工流程.md`，每篇参数记录进 `references/数据复盘表.md`。核心三点：参数微调（色温/阴影/颗粒/对比，数值打散）+ 空白处补少量彩铅排线 + 边角放专属小标记（每篇换位置）。

**数据复盘**：每篇发布后回填 `references/数据复盘表.md`，用首图划走率/完播率/评论量反向优化脚本与选题。

---

## 彩铅双分镜构图标准（保留）

**适用**：仅彩铅；**当故事总格数 >6 时自动启用双分镜**（≤6 格仍逐格单图）。脚本通过 `--split2` 或 story JSON `"split2": true` 开启并自动判定。

**核心规则**：
1. **一个 3:4 画布放两个分镜**：单张竖版 3:4（1024×1536）画布内划分上下两个叙事画面。
2. **分镜分隔 = 极淡浅灰细线**：约 `#d9d9d9` 的极淡浅灰色细线，**禁止粗黑漫画边框**（脚本用 Pillow 拼接，分隔线已内置该色）。
3. **每个分镜上方预留约1/3大留白区**：生图不画文字，分镜单元按35%顶部留白生成、拼接裁切后成图每格仍约30%纯纸色空白，画面上缘自然淡入留白，旁白后期自加，构图不拥挤。
4. **光影色调统一**：同张图上下两分镜、整套多张图都统一画纸纹理 + 光影（阶段3已锁一套）。
5. **细节完整不压缩**：分镜构图均衡、元素不拥挤，彩铅手绘细节完整清晰、不压缩笔触质感（脚本对每分镜单独 1024×1024 高清生成再拼接）。

**产物结构（以 8 格为例）**：
- 第1格 → `story_<id>.jpeg`：**封面，必为单图**
- 2+3、4+5、6+7 → `story_split_<a>_<b>.jpeg`：双分镜图（1024×1536）
- 末尾多余1格 → 单独成图

**旁白排版规范**：首选单行短句（≤20字）、每格顶部居中黑色粗体；特殊扩容最多两行、总字数≤35字，仅复杂剧情节点启用，不可通篇使用。

> **确定性加字（推荐，替代手动画字）**：出图后可直接用 `scripts/letter_baicat.py` 脚本，
> 按每格文字清单（manifest）把旁白以 Noto Sans CJK 简体黑粗体、顶部居中、逐字确定性地
> 绘制进留白区，并生成 ledger 校验（缺字 / 超 20 字 / 超 2 行或 35 字 / 放不下 均会报错）。
> 用法见「确定性加字」小节与 `references/lettering-manifest.example.json` 模板。

**信息分工**：
- 画面 + 后期加的旁白：承载**主线骨架**与视觉情绪。
- 作品正文（HTML/推文正文）：补充**人物背景、对话、细节铺垫、完整故事逻辑**。
- 图上方留白区只放主线短句（后期加字），背景与前因后果放正文文字，互补不重复。

**封面约束**：封面必须是单图，不能一图两格，用完整竖版3:4单格构图，保证首图信息聚焦、划走率低。

**发布辅助**：正文结尾放一个**开放式互动提问**，引导评论区作答、提升评论率。

---

## 整页多格模式（multipanel，可选）

**定位**：在"一图两格双分镜"之外，提供第三种构图——**一张 3:4 图装 3-5 格**，适合故事更长、
想让观众"一屏吃到更多观点"、降低逐格滑动流失的场景。**保留一图两格双分镜，二者互斥可切换**。

**适用**：仅彩铅；**需要显式开启**（story JSON `"multipanel": true` 或 CLI `--multipanel`），
总格数 ≥5 才真正启用；否则回退逐格单图。不写该字段就走原有双分镜/单图逻辑，完全兼容。

**排布风格**（两种都保留）：
- **free 自由艺术版（默认）**：非等分节奏 + 微旋转 + 留白错落，像有设计感的漫画页而非批量卡片网格。
- **regular 规整节奏版（降级）**：非等分但无旋转，顶部主格 + 下方小格，稳、平台兼容强。
- **自动降级**：free 布局拼接多次失败时自动回退 regular，保证出图不中断。
- 指定方式：story JSON `"multipanel_style": "free|regular"` 或 CLI `--mp-style`。

**格数范围与分页**：每页装 **3-5 格**；超过 5 格自动拆多页（如 8 格 → 封面 + 2 页，每页 4 格；
7 格 → 封面 + 2 页各 3 格）。封面（首格）始终单独出单图。

**产物结构（7 格为例，拼接版）**：
- 封面 → `story_<id>.jpeg`（单图）
- 每页 → `story_page_<n>.jpeg`（1024×1536 整页多格）
- 分镜单元保留 → `story_<pid>.jpeg`（方形，供审计/加字）

**实现路线二选一**：

① **拼接版（默认，`"page_native": false`/不写）**：每格仍 1024×1024 高清单独生成
（保细节、不压缩笔触），`compose_multipanel()` 按布局模板（`scripts/multipanel_layouts.py`）
缩放/旋转/覆盖裁切拼进整页，光影画纸整篇统一。
**已知风险（实测）**：布局模板格子宽高比 0.53~2.73 不等（源图为 1:1 方形），比例偏离 1 越远
裁切越多，部分格子上下/左右会被裁近半、画面不全——长镜头描述的故事慎用拼接版。

② **整页原生（推荐，`"page_native": true` 或 CLI `--page-native`，须配合 multipanel）**：
不逐格生成、不拼接。把该页全部 `scene` 合并成 "PAGE LAYOUT AND PANELS" 整页描述，
填入 STYLES.md 专用配方 `colored-pencil-page` 的【主体】，**一次调用 GPT 一次画出整页**
（1024×1536）。排版模板：顶部一个大横格 + 中部一排 2-3 小格 + 底部一个大横格，
格间白色边框 + 明显留白间距分隔，非等分、有漫画页呼吸感。
优点：无裁切、格子构图由 AI 整体设计（贴近真实漫画页）、一次生成省调用。
产物只有 `story_page_<n>.jpeg`，**无分镜单元文件**。

**加字**：
- 拼接版：`letter_baicat.py --auto` 已支持——对各分镜单元逐个确定性加字、再按同布局拼成
  带字整页（比在整页图上定位留白更可靠）。
- 整页原生：无单元文件，只能对整页图直接定位留白带加字；**留白带能否检测到取决于
  出图是否真留了干净空白**（生成前配方已要求每格上方留空，但需逐张验证，
  检测失败时备选：调 prompt 重出图 / 字放作品正文）。

---

## 生成工作流

### 1. 确认风格
默认彩铅；用户明确说"涂鸦"才用涂鸦；未指定→默认彩铅，不询问。

### 2. 写故事脚本
经阶段1-3已产出分镜，每格含：
- `scene`（画面）：一句话描述场景动作（英文，填入配方的 `【主体】`）。
- `text`（文字/旁白）：**两风格均不画进图里**，作剧情记录 + 彩铅光影情绪推断；生图只留顶部约1/3大留白，旁白后期自加。
- `text_in_image`（可选，默认 false）：**生图直接带汉字**——把每格 `text` 以一行**手写楷体**（带笔锋、黑墨）直接画进顶部留白区（GPT Image 2 渲染，个别字偶有瑕疵，不满意可关掉退回后期加字）。整页多格路线不支持（自动忽略并提示）。开启后**无需再跑 letter_baicat**（auto 模式检测到即跳过）。CLI 等价：`--text-in-image`。

情绪线设计：从一个状态渐变到另一个状态（如：期待→麻木→释然）。

### 3. 批量生成图片
用 `scripts/generate_story.py`（见阶段4命令）：
- story JSON 需含 `"style": "colored-pencil"`（或 doodle）、`"lighting": N`、`"paper": N`、`"split2": true`。
- `anchor` 可选；不填则第2格起自动用上一格输出当画风参考。
- 逐格调 GPT Image 2，保存 `{output_dir}/story_p1.jpeg` 等。

**画风一致性机制（连续分镜锁风格）**：`anchor` 字段指定画风锚点图，每格生成都把它作为 style-only 参考传入，锁定多格画风一致（只继承线条/配色/比例/氛围，不复制人物服装站位）；未指定时第2格起自动用上一格输出作参考。

**生图 API 细节**：
- 端点：`$IMAGE_API_URL/images/generations`（环境变量 `IMAGE_API_URL`）
- 模型：`gpt-image-2`；尺寸：`1024x1536`（竖屏条漫）；质量：`high`；返回 `b64_json`，base64 解码存 JPEG。
- split2 双分镜模式：每个分镜单元单独 `1024x1024` 方形高清生成，再 Pillow 拼接为 `1024x1536`（中间 #d9d9d9 淡灰细线）。

**IMAGE_API_URL 未设时的替代路线**：改用 `minis-model-use run --model gpt-image-2 --endpoint images-gen --input req.json --output out.png`（req.json 的 messages 放完整 prompt，generation_config 放 size/n）。

### 3.5 确定性加字（`letter_baicat.py`）

出图后给每张图的留白区确定性加旁白，逐字对准、可复现、可审计：

```bash
python3 /var/minis/skills/baicat/scripts/letter_baicat.py --manifest letter.json
```

manifest 结构（示例见 `references/lettering-manifest.example.json`）：
```json
{
  "font_file": "/usr/share/fonts/noto/NotoSansCJK-Bold.ttc",
  "font_index": 2,                     // 简体中文粗体
  "image_root": "<输出目录>",
  "images": [
    { "file": "story_split_2_3.jpeg", "layout": "split2",
      "texts": ["旁白二", "旁白三"] },   // split2=双分镜(上→下)；single=单图
    { "file": "story_p8.jpeg", "layout": "single", "texts": ["旁白八"] }
  ]
}
```

自动能力：
- **自适应定位留白区**（亮度检测，无需手填坐标，兼容单图/双分镜/真实纸纹理）
- **逐字字形检查**：缺字报错，不画豆腐块
- **规范校验**：单行>20字、两行>35字、>2行、留白放不下 → 均报错并给出提示
- **自动缩字号**：长文本会缩到能放进留白带（下限 26px）
- 每个字符用 `NotoSansCJK-Bold` index=2（简体粗体黑色），顶部居中

产物：`<原名>_lettered.jpg`（带字成品，保留无字底图）+ 同名 `.ledger.json`（记录源图哈希/字体/字号/坐标）。

> 未装中文字体时先 `apk add font-noto-cjk`，或 manifest 用 `font_file` 指定其它 CJK 字体路径。

**auto 批量模式（推荐，一条命令加整套图）**：
不需要手写 images 清单，直接从 story JSON + 产物目录自动按分镜顺序映射加字：
```bash
python3 /var/minis/skills/baicat/scripts/letter_baicat.py \
  --auto --story-json story.json --output-dir <产物目录>
```
自动识别：封面(首格,single)、双分镜(split2 上下两行)、整页多格(multipanel 各分镜单元)、
末尾多余单格(single)，把 story JSON 里每个 panel 的 `text` 填进对应分镜，批处理整套图。
也可用 manifest 的 `"auto": true` + `"story_json"` + `"output_dir"` 字段等价触发。

### 4. 组装图文故事页面
用 `templates/story_page.html` 生成最终 HTML：替换 `{{TITLE}}`、复制 panel 块、替换图片文件名、竖排图片模式；两风格生图均不画字，图片顶部留白区后期加字，HTML 可在图片下方放正文旁白/对话（信息分工）。图片与 HTML 同目录。

## 关键约束

- **两风格生图均不画文字、留白后期加** — doodle 与 colored-pencil 生图都**不画任何文字**，只在每格/分镜顶部留**约1/3大留白**（纯纸色、画面上缘自然淡入无硬分界）；旁白/台词后期手动添加（单行≤20字、复杂节点≤2行≤35字、顶部居中黑粗体；对话气泡可后期绘制再填字）。图是干净无字的底图。
- **text_in_image 可选带字路线** — 需要直接出带字成品时开 `"text_in_image": true`（或 `--text-in-image`）：旁白以**一行手写楷体**（带笔锋、黑墨，配彩铅怀旧调性；与后期加字路线的黑粗体不同路）画进顶部留白（prompt 末尾追加 NARRATION TEXT 覆盖块），同时构图占位符改为"留白区预留给一行文字"；letter_baicat --auto 检测到该开关自动跳过，防止二次叠字。整页多格路线不支持。
- **人物造型各格一致** — 同篇风格统一；连续分镜用 anchor 锚点图或逐格参考锁定。
- **彩铅>6格用双分镜** — 1格封面单图 + 其余两两拼一张3:4双分镜图（极淡浅灰细线，禁粗黑边框）；封面必为单图。
- **信息分工** — 画面+后期旁白承载主线骨架与视觉情绪；人物背景/对话/细节/完整逻辑放正文；正文结尾放开放式互动提问。
- **配方驱动** — 画风 prompt 存 `STYLES.md`，脚本只提取+填占位符，禁止手工缩写配方。
- **配方不锁角色** — 人物外貌由故事内容决定，各格保持一致即可。
- **留白为主** — 不要把画面填满；彩铅顶部约1/3是留给旁白的大留白，空比满好。
- **涂鸦只有一种红色**；**彩铅低饱和多色但克制**。

---

## 选题方向推荐（辅助功能）

仅用于阶段1判为 C/D 或用户无素材时。每次推荐3～5个**困境核**，每条含：普遍困境与人物关系；常识预期；可画细节方向；可用出口/临界点；适合的结局策略与情绪基调；避免落入的俗套。优先从恋爱、婚姻、亲子/原生家庭、职场/生存等高代入关系中找，但不当唯一范围。

## 最终自检

输出前确认：

- 故事不是纯观点、段子或事件流水账；
- 全局逻辑通过：人物动机、信息来源、时间地点、动作先后、因果和已发生事实均无矛盾；
- 有明确人物关系、具体困境和可画行动；
- 有足够支撑叙事的有效细节，不为凑数堆砌；
- 故事的主情绪明确，必要时说明情绪变化且不贪多；
- 结局与情绪基调一致，没用"你怎么看"硬拉评论；
- 不伪造真实来源，不把人物痛苦当猎奇；
- 编排时格数不超过15，没有无功能格；
- 阶段3已锁定彩铅视觉基调（光影+画纸序号），阶段4按此直接出图。

minis_url: minis://skills/baicat/SKILL.md

